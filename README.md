# sm-backend-track
1. Initialize project using npm init -y
2. Install express
3. create file app.js
4. add initial express code to run the node project
5. Create tasks array
6. Add get tasks endpoint and test it by curl
7. Add task by Id and test it by curl
8. Add Create new Task and test it by curl
9. Add Update Task and test it by curl
10. Add Delete Tasks and test it by curl
11. Install Swagger ui for api documentation and interaction
12. Commit at each step
13. Push to Github
